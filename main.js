"use strict"; 
//Loops

let arrayObject = [1, 4, 5, "Deepa", "Universe", true, "Miracles", 111];

//Printing the values of the loop using for Loop
for(let counter = 0; counter = arrayObject.length - 1; counter ++)
{
    console.log(counter);
}